# 🎬 Локальна відеотека на Flask

Невеликий Python-застосунок, який автоматично знаходить відеофайли у поточній директорії **та всіх піддиректоріях**, запускає локальний веб-сервер і дозволяє переглядати їх у браузері.

Для форматів, які браузер не підтримує напряму, використовується **on-the-fly перекодування через FFmpeg** без збереження файлів на диск.

---

## ✨ Можливості

- 🔍 Автоматичний пошук відео у папці запуску
- 🎞️ Підтримка форматів: `mp4`, `mkv`, `avi`, `mov`, `webm`, `m4v`, `ogv`
- 🌐 Відтворення у браузері через **Video.js**
- 🔄 Автоматичне перекодування (MKV / AVI / MOV → MP4)
- ⚡ Стрімінг відео напряму через FFmpeg
- 🎨 Адаптивний UI (Bootstrap 5)
- 🔐 Захист від directory traversal

---

## 🧠 Як працює програма

1. При старті скрипт сканує поточну директорію (`os.getcwd()`)
2. Знаходить усі відеофайли з дозволеними розширеннями
3. Flask генерує HTML-сторінку з відеоплеєрами
4. Якщо формат підтримується браузером — файл віддається напряму
5. Якщо ні — FFmpeg перекодовує відео у MP4 та стрімить у браузер

---

## 🖥️ Вимоги

### Обовʼязкові
- Python **3.9+**
- Будь-який сучасний браузер

### Python-залежності
- `flask`
- `imageio-ffmpeg`

> ⚠️ `imageio-ffmpeg` встановлюється **автоматично**, якщо не знайдений у системі.

---

## 📦 Встановлення

### 1️⃣ Отримати файл

```bash
git clone https://github.com/your-repo/video-library.git
cd video-library
```

або просто збережи `video-explorer.py` у папку з відео.

---

### 2️⃣ Встановити залежності (рекомендовано)

```bash
pip install flask imageio-ffmpeg
```

---

## ▶️ Запуск

⚠️ **Запускай скрипт з тієї директорії, де лежать відеофайли**

```bash
python video-explorer.py
```

Відкрий у браузері:

👉 http://127.0.0.1:777

---

## 📜 Ліцензія

MIT — використовуй без обмежень 🙂

## ⬇️ Завантаження (Windows)

Готовий виконуваний файл доступний у розділі **GitHub Releases**:

👉 **[Скачати Video Explorer для Windows (.exe)](https://github.com/jdayamx/video-explorer/releases/latest)**

### Як користуватись
1. Перейдіть за посиланням
2. Завантажте файл `VideoExplorer.exe`
3. Скопіюйте `.exe` у папку з відео
4. Запустіть файл — сервер стартує автоматично

> 💡 Програма портативна та не потребує встановлення.

## 📱 Перегляд відео з ПК на телефоні

Video Explorer запускає локальний веб-сервер, що дозволяє переглядати відео з компʼютера **на будь-якому пристрої в одній мережі**:
- 📱 смартфон
- 📲 планшет
- 💻 інший компʼютер
- 📺 Smart TV з браузером

### Як підключитись
1. Запустіть програму на ПК
2. Дізнайтесь IP-адресу компʼютера (наприклад `192.168.1.50`)
3. Відкрити у браузері телефону: `http://192.168.1.50:777`


